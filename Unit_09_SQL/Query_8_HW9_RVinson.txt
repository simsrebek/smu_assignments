--employee same last name count - used an alias 'frequency' in descending order


SELECT last_name, 
       Count(last_name) AS last_name_frequency 
FROM   employees 
GROUP  BY last_name 
ORDER  BY last_name_count DESC;